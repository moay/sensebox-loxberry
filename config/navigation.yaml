# Configure your admin navbar here, pass a title and a route to each item.
# The navbar won't be displayed if not items are found

home:
  title: navbar.settings
  route: settings
test:
  title: navbar.test_sensebox_connection
  route: senseboxTest
